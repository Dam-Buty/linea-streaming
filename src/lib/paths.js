const incoming = "./incoming";
const data = "./data";
const channels = `${data}/channels`;
const covers = `${data}/covers`;
const videos = `${data}/videos`;

module.exports = { incoming, videos, data, channels, covers };
